version: '3.8'

services:
  api:
    build: 
      context: .
      target: api
    ports:
      - "3001:3001"
    volumes:
      - ./api:/app
      - ./api/node_modules:/app/node_modules
    networks: 
      - calories-tracker
    environment:
      - DATABASE_URL=postgresql://ct-admin:cdadevops@db:5432/calories-tracker-db

  db: 
    image: postgres:16.0-alpine3.18
    env_file:
      - .env.local
    volumes:
      - db-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - calories-tracker

networks: 
  calories-tracker: 

volumes:
  db-data: 
